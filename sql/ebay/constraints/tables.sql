create schema ebay_constraints;

create table ebay_constraints.tf (
       tf varchar primary key
);

create table ebay_constraints.dis_en_abled (
       dis_en_abled varchar primary key
);

create table ebay_constraints.category_groups (
       groups varchar primary key
);

create table ebay_constraints.subscriptions (
       subscription varchar primary key
);       
