
create table amazon_music.valid_external_product_id_type (
     external_product_id_type varchar primary key
);

create table amazon_music.valid_feed_product_type (
     feed_product_type varchar primary key
);

create table amazon_music.valid_update_delete (
     update_delete varchar primary key
);

create table amazon_music.valid_binding (
     binding varchar primary key
);

create table amazon_music.valid_condition_type (
     condition_type varchar primary key
);

create table amazon_music.valid_product_tax_code (
     product_tax_code varchar primary key
);

create table amazon_music.valid_expedited_shipping (
     expedited_shipping varchar primary key
);

create table amazon_music.valid_standard_plus (
     standard_plus varchar primary key
);

create table amazon_music.valid_will_ship_internationally (
     will_ship_internationally varchar primary key
);

create table amazon_music.valid_fulfillment_center_id (
     fulfillment_center_id varchar primary key
);

create table amazon_music.valid_package_dimensions_unit_of_measure (
     package_dimensions_unit_of_measure varchar primary key
);

create table amazon_music.valid_package_weight_unit_of_measure (
     package_weight_unit_of_measure varchar primary key
);

create table amazon_music.valid_country_of_origin (
     country_of_origin varchar primary key
);

create table amazon_music.valid_cpsia_cautionary_statement (
     cpsia_cautionary_statement varchar primary key
);

create table amazon_music.valid_is_adult_product (
     is_adult_product varchar primary key
);

create table amazon_music.valid_subject_keywords (
     subject_keywords varchar primary key
);

create table amazon_music.valid_vinyl_record_details (
     vinyl_record_details varchar primary key
);

create table amazon_music.valid_audio_encoding_language (
     audio_encoding_language varchar primary key
);

create table amazon_music.valid_audio_encoding (
     audio_encoding varchar primary key
);

create table amazon_music.valid_format (
     format varchar primary key
);
