create table amazon_food_service_jansan.valid_nan (
     nan varchar primary key
);

create table amazon_food_service_jansan.valid_UPC (
     UPC varchar primary key
);

create table amazon_food_service_jansan.valid_Parent (
     Parent varchar primary key
);

create table amazon_food_service_jansan.valid_Accessory (
     Accessory varchar primary key
);

create table amazon_food_service_jansan.valid_USD (
     USD varchar primary key
);

create table amazon_food_service_jansan.valid_New (
     New varchar primary key
);

create table amazon_food_service_jansan.valid_choking_hazard_balloon (
     choking_hazard_balloon varchar primary key
);

create table amazon_food_service_jansan.valid_liters (
     liters varchar primary key
);

create table amazon_food_service_jansan.valid_meters (
     meters varchar primary key
);

create table amazon_food_service_jansan.valid_AD (
     AD varchar primary key
);

create table amazon_food_service_jansan.valid_milliliters_per_second (
     milliliters_per_second varchar primary key
);

create table amazon_food_service_jansan.valid_centimeters (
     centimeters varchar primary key
);

create table amazon_food_service_jansan.valid_carbon (
     carbon varchar primary key
);

create table amazon_food_service_jansan.valid_days (
     days varchar primary key
);

create table amazon_food_service_jansan.valid_bars (
     bars varchar primary key
);

create table amazon_food_service_jansan.valid_rpm (
     RPM varchar primary key
);

create table amazon_food_service_jansan.valid_minutes (
     minutes varchar primary key
);

create table amazon_food_service_jansan.valid_f (
     f varchar primary key
);

create table amazon_food_service_jansan.valid_kilograms (
     kilograms varchar primary key
);

create table amazon_food_service_jansan.valid_default (
     "default" varchar primary key
);

create table amazon_food_service_jansan.valid_PrivateLabel (
     PrivateLabel varchar primary key
);

create table amazon_food_service_jansan.valid_10 (
     "1.0" varchar primary key
);

create table amazon_food_service_jansan.valid_A_GEN_NOTAX (
     A_GEN_NOTAX varchar primary key
);

create table amazon_food_service_jansan.valid_Update (
     Update varchar primary key
);
