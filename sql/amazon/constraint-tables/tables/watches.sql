
create table amazon_watches.valid_external_product_id_type (
     external_product_id_type varchar primary key
);

create table amazon_watches.valid_update_delete (
     update_delete varchar primary key
);

create table amazon_watches.valid_currency (
     currency varchar primary key
);

create table amazon_watches.valid_condition_type (
     condition_type varchar primary key
);

create table amazon_watches.valid_product_tax_code (
     product_tax_code varchar primary key
);

create table amazon_watches.valid_offering_can_be_gift_messaged (
     offering_can_be_gift_messaged varchar primary key
);

create table amazon_watches.valid_offering_can_be_giftwrapped (
     offering_can_be_giftwrapped varchar primary key
);

create table amazon_watches.valid_is_discontinued_by_manufacturer (
     is_discontinued_by_manufacturer varchar primary key
);

create table amazon_watches.valid_missing_keyset_reason (
     missing_keyset_reason varchar primary key
);

create table amazon_watches.valid_website_shipping_weight_unit_of_measure (
     website_shipping_weight_unit_of_measure varchar primary key
);

create table amazon_watches.valid_target_audience_keywords (
     target_audience_keywords varchar primary key
);

create table amazon_watches.valid_specific_uses_keywords (
     specific_uses_keywords varchar primary key
);

create table amazon_watches.valid_fulfillment_center_id (
     fulfillment_center_id varchar primary key
);

create table amazon_watches.valid_package_dimensions_unit_of_measure (
     package_dimensions_unit_of_measure varchar primary key
);

create table amazon_watches.valid_package_weight_unit_of_measure (
     package_weight_unit_of_measure varchar primary key
);

create table amazon_watches.valid_prop_65 (
     prop_65 varchar primary key
);

create table amazon_watches.valid_cpsia_cautionary_statement (
     cpsia_cautionary_statement varchar primary key
);

create table amazon_watches.valid_department_name (
     department_name varchar primary key
);

create table amazon_watches.valid_band_material_type (
     band_material_type varchar primary key
);

create table amazon_watches.valid_band_size (
     band_size varchar primary key
);

create table amazon_watches.valid_band_width_unit_of_measure (
     band_width_unit_of_measure varchar primary key
);

create table amazon_watches.valid_band_color (
     band_color varchar primary key
);

create table amazon_watches.valid_clasp_type (
     clasp_type varchar primary key
);

create table amazon_watches.valid_case_material_type (
     case_material_type varchar primary key
);

create table amazon_watches.valid_case_diameter_unit_of_measure (
     case_diameter_unit_of_measure varchar primary key
);

create table amazon_watches.valid_metal_stamp (
     metal_stamp varchar primary key
);

create table amazon_watches.valid_dial_color (
     dial_color varchar primary key
);

create table amazon_watches.valid_bezel_material_type (
     bezel_material_type varchar primary key
);

create table amazon_watches.valid_bezel_function (
     bezel_function varchar primary key
);

create table amazon_watches.valid_dial_window_material_type (
     dial_window_material_type varchar primary key
);

create table amazon_watches.valid_display_type (
     display_type varchar primary key
);

create table amazon_watches.valid_item_shape (
     item_shape varchar primary key
);

create table amazon_watches.valid_watch_movement_type (
     watch_movement_type varchar primary key
);

create table amazon_watches.valid_calendar_type (
     calendar_type varchar primary key
);

create table amazon_watches.valid_water_resistance_depth_unit_of_measure (
     water_resistance_depth_unit_of_measure varchar primary key
);

create table amazon_watches.valid_resale_type (
     resale_type varchar primary key
);

create table amazon_watches.valid_warranty_type (
     warranty_type varchar primary key
);

create table amazon_watches.valid_special_features (
     special_features varchar primary key
);
